apiVersion: v1
kind: Service
metadata:
  name: {{ include "redzine.fullname" . }}
  labels:
    {{- include "redzine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "redzine.selectorLabels" . | nindent 4 }}
