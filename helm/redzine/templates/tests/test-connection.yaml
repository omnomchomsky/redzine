apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "redzine.fullname" . }}-test-connection"
  labels:
    {{- include "redzine.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "redzine.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
